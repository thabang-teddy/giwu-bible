version: '3.8'

services:
  website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: giwu_website
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "8080:8111"
    networks:
      - webnet

networks:
  webnet:
    external: true
